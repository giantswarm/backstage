diff --git a/node_modules/@assistant-ui/store/dist/useAuiEvent.js b/node_modules/@assistant-ui/store/dist/useAuiEvent.js
index 8ea5500..8485cc4 100644
--- a/node_modules/@assistant-ui/store/dist/useAuiEvent.js
+++ b/node_modules/@assistant-ui/store/dist/useAuiEvent.js
@@ -1,9 +1,17 @@
-import { useEffect, useEffectEvent } from "react";
+import { useEffect, useCallback, useRef, useInsertionEffect } from "react";
 import { useAui } from "./useAui.js";
 import { normalizeEventSelector } from "./types/events.js";
+// Polyfill for useEffectEvent which is not yet exported from stable React
+function useEffectEventPolyfill(callback) {
+    const ref = useRef(callback);
+    useInsertionEffect(() => {
+        ref.current = callback;
+    });
+    return useCallback((...args) => ref.current(...args), []);
+}
 export const useAuiEvent = (selector, callback) => {
     const aui = useAui();
-    const callbackRef = useEffectEvent(callback);
+    const callbackRef = useEffectEventPolyfill(callback);
     const { scope, event } = normalizeEventSelector(selector);
     useEffect(() => aui.on({ scope, event }, callbackRef), [aui, scope, event]);
 };
