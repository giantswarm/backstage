diff --git a/node_modules/@apidevtools/json-schema-ref-parser/dist/lib/resolvers/http.js b/node_modules/@apidevtools/json-schema-ref-parser/dist/lib/resolvers/http.js
index 917fb4c..8ca9fc6 100644
--- a/node_modules/@apidevtools/json-schema-ref-parser/dist/lib/resolvers/http.js
+++ b/node_modules/@apidevtools/json-schema-ref-parser/dist/lib/resolvers/http.js
@@ -171,7 +171,9 @@ async function get(u, httpOptions, resolvedAddresses) {
 async function createPinnedTransport(requestUrl, resolvedAddresses) {
     // Keep the Node-only implementation out of browser module graphs.
     const undiciModuleName = "undici";
-    const undici = (await import(undiciModuleName));
+    // Patched: webpackIgnore keeps webpack from treating this expression import as a
+    // "Critical dependency" warning, which is fatal under CI=true. See patches/README.md.
+    const undici = (await import(/* webpackIgnore: true */ undiciModuleName));
     const expectedHostname = normalizeLookupHostname(requestUrl.hostname);
     const pinnedAddresses = resolvedAddresses.map(({ address, family }) => ({ address, family }));
     const lookup = (hostname, options, callback) => {
diff --git a/node_modules/@apidevtools/json-schema-ref-parser/dist/lib/util/url.js b/node_modules/@apidevtools/json-schema-ref-parser/dist/lib/util/url.js
index 4e362be..fcbfb75 100644
--- a/node_modules/@apidevtools/json-schema-ref-parser/dist/lib/util/url.js
+++ b/node_modules/@apidevtools/json-schema-ref-parser/dist/lib/util/url.js
@@ -257,7 +257,9 @@ export async function resolveUrlSafety(path) {
     // Keep the Node-only dependency out of browser module graphs. The import is
     // never evaluated outside Node.js.
     const dnsModuleName = "node:dns/promises";
-    const dns = (await import(dnsModuleName));
+    // Patched: webpackIgnore keeps webpack from treating this expression import as a
+    // "Critical dependency" warning, which is fatal under CI=true. See patches/README.md.
+    const dns = (await import(/* webpackIgnore: true */ dnsModuleName));
     const addresses = await dns.lookup(normalizeHostname(parsedUrl.hostname), {
         all: true,
         verbatim: true,
