---
'@giantswarm/backstage-plugin-platform-capabilities': patch
---

Capabilities tab: a choice not on record that the form has no field for — a list of objects such as the portal's friendly labels, friendly annotations or Flux repository patterns — is named by its key (_Friendly labels_) like every other choice, instead of by its dotted field.
