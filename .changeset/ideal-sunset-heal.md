---
'@giantswarm/backstage-plugin-ai-chat': minor
---

Add conversation history page with table listing conversations (delete, rename, batch delete), tabs on the AI chat page, and a conversation history view in the drawer with active-conversation highlight. Includes a shared QueryClient between page and drawer, a conversation API client with React Query hooks, and dialogs for delete, batch delete, and rename.
