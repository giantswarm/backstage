---
'@giantswarm/backstage-plugin-ui-react': minor
---

Add `Gate`, the dashed-border auth gate with a lock icon and an optional action,
moved here from the muster plugin so the gs installation-inventory gate can use
the same box.
