---
'@giantswarm/backstage-plugin-gs': patch
---

Rename the deployment detail "Gateways" tab to "Routes" (route path
`/routes`), and rename the underlying `DeploymentGateway` component to
`DeploymentRoutes` to match.
