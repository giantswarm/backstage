---
'@giantswarm/backstage-plugin-ui-react': patch
---

Add `passwordManagerIgnoreProps` utility for suppressing password manager autofill on form fields. Add `height` and `maxHeight` options to the `YamlEditor` component.
