---
'app': patch
---

Update Backstage to 1.49.3 and fix entity relation cards (Has Components, Has Resources, etc.) showing only one row by forcing deduplication of @backstage/plugin-catalog-react to 2.1.1, which contains the EntityDataTable pagination fix.
