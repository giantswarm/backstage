---
'@giantswarm/backstage-plugin-gs': patch
---

The Cluster access popover opens above its sidebar item in the mobile sidebar,
where the item is a full-width row in the Menu, instead of to the item's right.
