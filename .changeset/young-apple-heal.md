---
'@giantswarm/backstage-plugin-ai-chat-backend': patch
---

Add optional `aiChat.systemPrompt` config to override the built-in AI chat system prompt. MCP-specific additions (muster prompt, failed-server notes) are still appended.
