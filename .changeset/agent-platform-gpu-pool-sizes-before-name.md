---
'@giantswarm/backstage-plugin-agent-platform': patch
---

Add GPU node pool: the node sizes, their prices and the presets each hosts appear as soon as a cluster is picked, before the pool is named — the sizing dry run carries a placeholder name until then; the name only enables Review.
