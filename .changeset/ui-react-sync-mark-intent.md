---
'@giantswarm/backstage-plugin-ui-react': minor
---

`syncMarkIntent(mark)`: the `StatusLabel` intent of a `SyncMark`, for a
label that names in words the state a table's `SyncMarkIcon` shows, so the
two colour alike.
