---
'@giantswarm/backstage-plugin-platform-capabilities': patch
---

The Installations table's capability icons are the portal's shared
`SyncMarkIcon`, coloured from the bui intent tokens so they follow the
theme; the marks, glyphs and words are unchanged.
