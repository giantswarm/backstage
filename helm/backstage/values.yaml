name: backstage
namespace: backstage

userID: 1000
groupID: 1000

image:
  name: backstage
  repository: giantswarm/backstage

port: 7007
hostnames: []

registry:
  domain: quay.io

circleci:
  apiToken: ""

githubAuthCredentials:
  clientID: ""
  clientSecret: ""

githubAppCredentials: ""

catalog: ""

oauth2Proxy:
  name: oauth2-proxy
  port: 4180
  version: v5.1.0
  image:
    repository: giantswarm/oauth2_proxy

  secrets:
    client_id: ""
    client_secret: ""
    cookie_secret: ""

  # OAuth2 proxy will use GitHub as an identity provider.
  github:
    # Name of the organization a user must be a member of.
    organizationName: ""
    # Name of a team the user must be a member of.
    # Use the all lowercase slug spelling here.
    teamName: ""
