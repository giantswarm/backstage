{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$TOOL_INPUT\" | jq -r '.command' | grep -q 'git commit'; then yarn prettier:fix > /dev/null 2>&1 && git add -u; fi"
          }
        ]
      }
    ]
  }
}
