# Builds the ui-react Storybook (docs for @giantswarm/backstage-plugin-ui-react)
# and enforces the story coverage gate on every PR; publishes the static site to
# GitHub Pages on merge to main.
#
# Hand-authored (not devctl-generated), so it is safe to edit here.
name: Storybook

# Only the ui-react library, the Storybook config/tooling, and dependency
# manifests affect this site — so scope the PR/push triggers to those paths and
# don't make unrelated (e.g. backend-only) PRs pay for a full install + build.
# merge_group is left unscoped so the merge queue always gets a green signal.
on:
  pull_request:
    paths:
      - 'plugins/ui-react/**'
      - '.storybook/**'
      - 'scripts/check-story-coverage.mts'
      - 'tsconfig.storybook.json'
      - '.github/workflows/storybook.yaml'
      - 'package.json'
      - 'yarn.lock'
  merge_group:
  push:
    branches:
      - main
    paths:
      - 'plugins/ui-react/**'
      - '.storybook/**'
      - 'scripts/check-story-coverage.mts'
      - 'tsconfig.storybook.json'
      - '.github/workflows/storybook.yaml'
      - 'package.json'
      - 'yarn.lock'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: storybook-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Runs on every PR (and on main): the story coverage gate + a full Storybook
  # build. A broken story or an undocumented exported component fails here.
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: Check out code
        uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: yarn
      - name: Install dependencies
        run: yarn install --immutable
      - name: Type-check Storybook config and scripts
        run: yarn typecheck:storybook
      - name: Check story coverage
        run: yarn storybook:coverage
      - name: Build Storybook
        run: yarn storybook:build
      - name: Upload Pages artifact
        # Only needed for the deploy job (push to main).
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: actions/upload-pages-artifact@v3
        with:
          path: storybook-static

  # Publishes the built site to GitHub Pages, only on merge to main.
  deploy:
    needs: build
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-24.04
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
